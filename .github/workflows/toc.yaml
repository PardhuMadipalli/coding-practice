on: push
name: TOC Generator
jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
        with:
          CREATE_PR: true
          TARGET_PATHS: javapractice/index.md
          GITHUB_TOKEN: ${{ secrets.TOC_GENERATOR }}
          MAX_HEADER_LEVEL: 6